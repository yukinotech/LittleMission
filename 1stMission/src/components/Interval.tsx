import * as React from 'react'
import styled from 'styled-components';

let IntervalDiv = styled.div`
    padding-top: 0.6rem;
    background-color: rgb(244, 245, 246);
`

export default function(){
    return (
        <IntervalDiv/>
    )
}